# Deployment do Apache HTTPD - Web Solutions Ltda.
# Este manifesto define como o Kubernetes deve gerenciar os pods do Apache

apiVersion: apps/v1
kind: Deployment
metadata:
  name: apache-deployment
  labels:
    app: apache
    project: web-solutions
spec:
  replicas: 2  # Número de réplicas para alta disponibilidade
  selector:
    matchLabels:
      app: apache
  template:
    metadata:
      labels:
        app: apache
        project: web-solutions
    spec:
      containers:
      - name: apache
        image: httpd:alpine  # Imagem otimizada baseada em Alpine
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 3
